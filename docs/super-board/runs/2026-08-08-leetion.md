# super-board run — leetion — 2026-08-08

- Config: `.claude/super-board/configs/leetion.json` (variant: full, backend: workflow, tier: medium, max_workers: 3, human_approves_merge: true)
- Columns at start: QA ×1 (#1), Done ×3 (#2 #3 #4), Backlog ×1 (#5)
- Orchestrator session: interactive (workflow backend), lock `.claude/super-board/inflight/workflow-wave.lock`
- Context: #1 was bounced out of Done by the owner after Chrome testing on 2026-08-07/08; the post-bounce rebuild (`51a6697`, `a876d34`, `f5f7453`) landed before this run and left the card in QA. This run picks it up at the Tester.

## Wave 1 — run `wf_bc2a4b6e-99b` (~29 min, 2 agents, 0 errors)

| Card | Lanes run | Final status | Column | Detail |
|---|---|---|---|---|
| #1 | qa:advanced → review:advanced | advanced | Done | QA pass v2: 7/7 ACs pass (4 as written, 3 as owner-superseded). Reviewer approved pass 1: Gate 1 clean, truth 88/100, all gates rerun green (27/27 + 79/79 + 41/41 + `node --check` + `build.sh`). PR #6 left Ready for human merge. |

Notable within the wave (both handled inside the lanes, no orchestrator intervention):

- **PR #6 arrived `CONFLICTING`.** #7, #8 and #9 all merged into `main` while #1 sat in Done, and all three touch the same functions. The Tester merged `main` into the branch (`32d5185`); PR is now `MERGEABLE / CLEAN`. One hunk was non-mechanical: #8's early return for the "no snapshots, no notes" path fired after the Attempts PATCH without carrying `attempts`, which would have left a staged edit displayed as unsaved forever after it had actually been written — fixed with a `withAttempts` wrapper covering every post-PATCH return. The Reviewer independently confirmed both post-PATCH returns go through the wrapper.
- **Two suites went red on the merged tree; both were fixture gaps, not regressions.** The issue-1 fake database reported columns with no `type` (read as "every column mismatched" by #9's schema inspector); the issue-2 mock had no `GET pages/{id}` route for #1's server-fresh read. Both fixed in QA-owned files. The Reviewer specifically checked that no product code was bent to make a test pass, and that two regenerated issue-2 evidence JSONs are write-only dumps, never read back as golden comparisons.

## Run end — board drained (2026-08-08 ~00:55 CDT)

- Final columns: Done ×4 (#1 #2 #3 #4), Backlog ×1 (#5 — human staging, untouched by design).
- Done condition: wave planner returned empty with Building/QA/Review at 0.
- Halt gates fired: none. Blocked: 0. Rebuild loops: 0 (bounce came from the human, not a lane).
- **Human merge queue: PR #6 (issue #1)** — approved, `MERGEABLE / CLEAN`, already contains `main`. Last of the four branches; #7/#8/#9 merged before this run.
- **4 non-blocking findings** recorded by the Reviewer in the PR #6 comment, no follow-up card filed: dead call to the deleted `formatNumber`; Firefox stepper-styling gap; schema-mismatch guard no longer covering `Attempts` on the update path; staged-save vs session-increment semantics.
- Board curation this session (owner request, outside the lanes): **#5 revised** — was "Sync Attempts with real LeetCode submission count"; now "Add a Submissions property that syncs with the real LeetCode submission count". The sync targets a new Leetion-owned `Submissions` Number property and leaves the user-owned `Attempts` untouched, which dissolves the confirm-before-overwrite design problem the original ticket carried. Added requirements: `DATABASE_SCHEMA` + README table change together (the #4 invariant), and a harness assertion that syncing provably does not disturb `Attempts`. Stays in Backlog, sequenced after #1 merges.
- Resume command: `super-board run` (state lives on the board).
